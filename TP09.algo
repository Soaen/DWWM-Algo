algo TP9

Variables
nom1,nom2, nom3 : chaine

Début

  ÉCRIRE("Saisir un premier nom")
  LIRE(nom1)

  ÉCRIRE("Saisir un deuxième nom")
  LIRE(nom2)

  ÉCRIRE("Saisir un troisième nom")
  LIRE(nom3)

Si (nom1 <= nom2) ET (nom2 <= nom3) alors
    Écrire("Les noms sont dans l'ordre alphabétique")
Sinon
    Écrire("Les noms ne sont pas dans l'ordre alphabétique")
FinSi

Fin